// Magic: 995a5eb4
// Automatically generated PSI dialog (PSI version 20.5.584)
// Tue Jun  3 14:00:00 2025


f_tmp = FLOAT_FIELD;	// dialog.top.ui

environment.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) HSTRETCH
    { WIDTH(0.18) }

    environment.label = PARM_LABEL_SIMPLE("Environment", "environment") WIDTH(1.5);
    { WIDTH(0.09) }

    environment.menu;

    { HSTRETCH }

    { WIDTH(0.1) }
}
clear.row.join =		
{

  LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)
  HMARGIN(0.18, 0.1) NO_MARGIN_SPACING_SCALE

clear.gad = ROW
{
    JUSTIFY(left, center)
    {  }

    { WIDTH(1.5) }
    { WIDTH(0.1) }

    PARM_BUTTON("clear", "Reload")
	push VALUE(clear.val);

    {  }
}

dir.gad = 
{
 LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)
 {  }

 dir.label = PARM_LABEL("Directory", "dir") ;

 PARM_FIELD (pathstring, "dir") LABELREF(dir.label)
	VALUE(dir.val) HSTRETCH;

    dir.menu SIZE(0.26, 0.26) LOOK(menuiconbutton);

 ACTION_ICONBUTTON "BUTTONS_chooser_file" VALUE(dir.plus) LOOKFLAT
    HELP("Open floating file chooser") UI_FLAT_ICON_BTN_SIZE_SMALL;

 {  }
}
}	// Template join.bot

mode.row.join =		
{

  LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)
  HMARGIN(0.18, 0.1) NO_MARGIN_SPACING_SCALE

mode.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) 
    {  }

    mode.label = PARM_LABEL_SIMPLE("Mode", "mode") WIDTH(1.5);
    { WIDTH(0.09) }

    mode.menu;

    {  }

    {  }
}

input7.gad = 
{
    LAYOUT(horizontal) JUSTIFY(left, center) 
    {  }

    input7.label = PARM_LABEL_SIMPLE("Layer", "input7") ;
    { WIDTH(0.09) }

    input7.menu;

    {  }

    {  }
}
}	// Template join.bot

Base_enable.row.join =		
{

  LAYOUT(horizontal) HSTRETCH JUSTIFY(left, center)
  HMARGIN(0.18, 0.1) NO_MARGIN_SPACING_SCALE

Base_enable.gad = ROW
{
    
    {  }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("Base_enable", "Layer 1 Base") toggle VALUE(Base_enable.val);

    {  }

    {  }
}

Manual_enable.gad = ROW
{
    
    {  }

    PARM_BUTTON("Manual_enable", "Layer 2 Manual") toggle VALUE(Manual_enable.val);

    {  }

    {  }
}

Procedural_enable.gad = ROW
{
    
    {  }

    PARM_BUTTON("Procedural_enable", "Layer 3 Procedural") toggle VALUE(Procedural_enable.val);

    {  }

    {  }
}

Material_enable.gad = ROW
{
    HSTRETCH
    {  }

    PARM_BUTTON("Material_enable", "Layer 4 Material") toggle VALUE(Material_enable.val);

    { HSTRETCH }

    {  }
}
}	// Template join.bot


folder0.gad = 

GROUP simple "Houdini" flat 
{ 
    VALUE(folder0.gad.val) LAYOUT(vertical) HSTRETCH 
	    Base_enable.row.join;		
} 

folder2.gad = 

{
    VALUE(folder2.gad.val) LAYOUT(vertical) HSTRETCH SPACING(0.02)
	    mode.row.join;		
} 

folder4.gad = 

{
    VALUE(folder4.gad.val) LAYOUT(vertical) HSTRETCH SPACING(0.02)
	    clear.row.join;		
	    folder2.gad;		
	    folder0.gad;		
} 

folder1.gad = 

GROUP simple "Settings" flat 
{ 
    VALUE(folder1.gad.val) LAYOUT(vertical) HSTRETCH 
	    environment.gad;		
	    folder4.gad;		
} 

dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH 
    folder1.gad; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val_) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
