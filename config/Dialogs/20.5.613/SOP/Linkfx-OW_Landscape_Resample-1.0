// Magic: 6f45ead
// Automatically generated PSI dialog (PSI version 20.5.613)
// Fri Jul  4 18:35:06 2025


f_tmp = FLOAT_FIELD;	// dialog.top.ui

enable.gad = ROW
{
    HSTRETCH
    { WIDTH(0.18) }

    { WIDTH(1.5) }
    { WIDTH(0.08) }

    PARM_BUTTON("enable", "Enable Resample") toggle VALUE(enable.val);

    { HSTRETCH }

    { WIDTH(0.1) }
}

gridspacing.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 gridspacing.label = PARM_LABEL("Grid Spacing", "gridspacing") WIDTH(1.5);
 PARM(float, 1, "gridspacing", none, none) RANGE(0,8)
	LOCK_RANGE_MIN LABELREF(gridspacing.label) HSTRETCH;

  { WIDTH(0.1) }
}

filterscale.gad = 
{

 LAYOUT(horizontal) HSTRETCH
 { WIDTH(0.18) }

 filterscale.label = PARM_LABEL("Filter Scale", "filterscale") WIDTH(1.5);
 PARM(float, 1, "filterscale", none, none) RANGE(0,5)
	LOCK_RANGE_MIN LABELREF(filterscale.label) HSTRETCH;

  { WIDTH(0.1) }
}

folder0.gad = 
GROUP collapsible "Resample" flat TOGGLEREF(enable.gad) PARMREF(gridspacing.gad) 
{
    VALUE(folder0.gad.val) LAYOUT(vertical) HSTRETCH
	    gridspacing.gad;		
	    filterscale.gad;		
} 

dialog_norm.gad = 
{
    LAYOUT(vertical) SPACING(0.02) HSTRETCH 
    enable.gad; 
    folder0.gad; 
} 
// dialog.bot.ui

extrastuffstowbar_minimize.val := 1;

dialog = OPENNOTIFIER
{
    LAYOUT(vertical) SPACING(0.02) LOOK(bg) VALUE(open.val_) HSTRETCH

    { HEIGHT(1p) LOOK(bg) HSTRETCH }

    dialog_norm.gad;
}
